export default async function handler(req,res){try{const raw=req.query.url;if(!raw)return res.status(400).send("Missing image URL");const u=new URL(raw);if(!/^https?:$/.test(u.protocol))return res.status(400).send("Invalid URL");const r=await fetch(u.toString(),{headers:{"User-Agent":"Mozilla/5.0 AppleWebKit/537.36 Chrome/126 Safari/537.36","Accept":"image/avif,image/webp,image/apng,image/svg+xml,image/*,*/*;q=0.8"}});if(!r.ok)return res.status(502).send("Image fetch failed");res.setHeader("Content-Type",r.headers.get("content-type")||"image/jpeg");res.setHeader("Cache-Control","public,max-age=3600");return res.status(200).send(Buffer.from(await r.arrayBuffer()))}catch(e){return res.status(500).send("Could not proxy image")}}
